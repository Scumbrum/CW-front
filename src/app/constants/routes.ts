export enum ROUTES {
  AUTH = 'auth',
  REGISTRATION = 'register',
  HOME = '',
  PROFILE = 'profile',
  OTHER_PROFILE = 'profile/:id',
  STREAM_DETAILS = 'stream/:id',
  COURSE_DETAILS = 'course/:id'
}
